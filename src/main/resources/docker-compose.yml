version: '3'

volumes:
  v-demo:

services:
  db-demo:
    image: postgres
    ports:
      - '25432:5432'
    volumes:
      - 'v-demo:/var/lib/postgresql/data'
      - './dbinit:/docker-entrypoint-initdb.d'
    environment:
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=postgres